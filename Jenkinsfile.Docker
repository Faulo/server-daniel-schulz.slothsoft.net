pipeline {
	dockerfile {
        filename 'Dockerfile'
        dir '.'
        label 'latest'
        additionalBuildArgs  ''
        args ''
    }
    stages {
        stage('Docker Build') {
            steps {
                sh 'composer install --dev'
            }
        }
    }
}