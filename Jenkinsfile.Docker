pipeline {
	agent {
		label 'jenkins && docker'
	}
    stages {
        stage('Docker Build') {
            steps {
                sh 'docker build -t daniel-schulz.slothsoft.net:latest .'
            }
        }
    }
}